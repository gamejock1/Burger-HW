<h1>Burgers!</h1>

<h2>Burgers that are not eaten:</h2>

<ol>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger_block devoured=false}}
    {{/unless}}
  {{/each}}
</ol>

<h2>Burgers that are eaten:</h2>

<ol>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger_block devoured=true}}
    {{/if}}
  {{/each}}
</ol>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="newBurger">
  </div>

  <br>

  <button class="btn btn-primary" id="submit" type="submit">Add Burger</button>
</form>
